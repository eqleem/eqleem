import { contentTypeBySlug } from './page.js';

/** Content-type meta for newsletter shell / nav. Live data lives in useNewsletterStore. */
export const newsletterType = contentTypeBySlug('newsletter');

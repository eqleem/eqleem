import { contentTypeBySlug } from './page.js';

/** Content-type meta for digital-services shell / nav. Live data lives in useDigitalServicesStore. */
export const digitalServicesType = contentTypeBySlug('digital-services');
